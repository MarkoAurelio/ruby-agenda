default: &default
  adapter: postgresql
  encoding: unicode
  username: marko # Substituia pelo nome de usuário local, se aplicável
  password: # Substituia pela senha de usuário local, se aplicável
  host: localhost
  pool: 5

development:
  <<: *default
  database: ruby_db

test:
  <<: *default
  database: ruby_db_test


